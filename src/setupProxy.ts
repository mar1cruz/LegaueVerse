// const { createProxyMiddleware } = require('http-proxy-middleware');
//
// module.exports = function(app:any) {
//     app.use( '',
//         createProxyMiddleware({
//             target: 'http://185.244.50.145', // Адрес вашего сервера, куда будет перенаправлен запрос
//             changeOrigin: true, // Установите это значение в true, если хост вашего целевого сервера отличается от хоста, который делает запрос
//         })
//     );
// };

export const a ={}